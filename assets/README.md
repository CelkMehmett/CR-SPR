Assets for social preview

Place a 1200×630 PNG at `assets/preview.png`. This image will be used by Open Graph (Twitter/LinkedIn) when sharing the demo page.

Suggested filename: `preview.png` (1200×630). If you prefer another name, update `crispr-demo.html` and `index.html` meta tags accordingly.

Example image creation (local):
```
# use any image editor to create 1200x630 PNG and copy here
```
